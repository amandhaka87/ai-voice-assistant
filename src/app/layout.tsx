import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "AI Voice Assistant",
  description: "Futuristic AI voice assistant — talk to OpenAI, Gemini, or Claude with animated robots",
  keywords: ["AI", "voice assistant", "OpenAI", "Gemini", "Claude", "speech recognition"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
